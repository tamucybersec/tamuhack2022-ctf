# Build docker image
docker build -t secret_number .

# Run docker image
docker run -p 8000:80 -d --name web_chall_secret_num secret_number

# Remove container when done
docker container stop web_chall_secret_num
docker container rm web_chall_secret_num
